# From JavaInternship-api-gateway/: build all Spring services from sibling repos.
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up --build

services:
  user-service:
    build:
      context: ../JavaInternship-user-service
      dockerfile: Dockerfile

  auth-service:
    build:
      context: ../Javaintership-auth-service
      dockerfile: Dockerfile

  order-service:
    build:
      context: ../JavaInternship-order-service
      dockerfile: Dockerfile

  api-gateway:
    build:
      context: .
      dockerfile: Dockerfile
